name: Bundle Maven CI

on: [workflow_dispatch, workflow_call, push, pull_request]

jobs:
  call-workflow:
    uses: Forsakringskassan/.github/.github/workflows/bundle-maven-ci.yaml@main
    secrets:
      PACKAGES_RW_ACTOR: ${{ secrets.PACKAGES_RW_ACTOR }}
      PACKAGES_RW_TOKEN: ${{ secrets.PACKAGES_RW_TOKEN }}

  validate-config:
    uses: Forsakringskassan/rimfrost-framework-regel/.github/workflows/validate-config.yaml@798686a2c3a907f1f6745093ab1a202d0a5e2cf1 # 1.2.4
    with:
      framework-ref: '1.2.3'